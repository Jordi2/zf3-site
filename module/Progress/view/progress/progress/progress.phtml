<h1>Progression Chart</h1>

<p>Exercises that build on one another are called progressions, with each exercise requiring more mobility and/or strength than the previous. 
The progressions below are further sorted by categories of movements that tend to have overlap in muscles used. For a full body exercise practice at least one exercise for each category!</p>
<div id="progressionFilters">
    
    <form method="get" action="<?= $this->url('progress', ['action' => 'progress']) ?>">      
    <fieldset>      
        <legend>Filters:</legend>  
        <?php
        foreach ($this->classifications as $classification) : 
        ?>
            <input style="display:none !important;" type="checkbox" name="classification<?= $classification['id']; ?>" <?= $classification['checked']; ?> value="<?= $classification['id']; ?>">
        <?php
        endforeach;
        ?>
         <?php
        foreach ($this->tags as $tag) : 
        ?>
            
            <input type="checkbox" id="tag<?= $tag['id']; ?>" class="checkbox" name="tag<?= $tag['id']; ?>" <?= $tag['checked']; ?> value="<?= $tag['id']; ?>"/>
            <label for="tag<?= $tag['id']; ?>"><?= $tag['name']; ?></label>
        <?php
        endforeach;
        ?>
            </br>

        <button type="submit" class="btn btn-light" style="border-color: black;">Submit</button>
        <div style="display:none;" class="sumbit-button">Submit</div>
    </fieldset>      
</form>
</div>
<div class="clear"></div>

<div id="progressionContent">
    <?php 
    echo $this->partial('application/index/progressionChartPartial', 
            ['exerciseProgressions'=>$this->exerciseProgressions, 'classifications'=>$this->classifications]); 
    ?>
</div>

<ul class="dropdown-menu fade-up root">
                            <li><a class="dropdown-item">Variants:</a></li>
                            
                <li>
                    <div class="rank1 variant">Vertical Row</div>
                </li>
                
                <li>
                    <div class="rank1 variant">Incline Row</div>
                </li>
                
                        </ul>

    <div class="popover container-drop right in" style="display: none !important; top: 374.5px; left: 424px; display: block;">
        <div class="arrow"></div>
        <div>
            <h3 class="dropdown-title">Variants:</h3>
            <div class="dropdown-content">
                <ul >
                    
                    
                    <li>Vertical Row</li>
                    <li>Incline Row</li>
                </ul>
            </div>
        </div>
    </div>

<div class="clear"></div>
<svg height="210" width="500">
  <line x1="125" y1="0" x2="125" y2="200" style="stroke:black;stroke-width:40" />
  <line x1="50" y1="100" x2="200" y2="100" style="stroke:black;stroke-width:40" />
  <circle cx="50" cy="50" r="40" stroke-width="4" fill="black" />
  Sorry, your browser does not support inline SVG.
</svg>